#pragma once

/**
 *  \brief Internally initializes the game's visuals data structure.
 *
 *  \return Returns ERR if an error occurred, or OK.
 *
 *  \sa init_visuals_data_structure()
 */
int init_visuals_data_structure(void);
